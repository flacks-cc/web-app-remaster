import { LimitDigitsDirective } from './limit-digits.directive';

describe('LimitDigitsDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitDigitsDirective();
    expect(directive).toBeTruthy();
  });
});
